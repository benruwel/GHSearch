import { RepoHighlighterDirective } from './repo-highlighter.directive';

describe('RepoHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new RepoHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
